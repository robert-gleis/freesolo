# Repository Conventions

- TypeScript with ES modules (`"type": "module"`, NodeNext resolution).
- Tests use Vitest; follow red-green-refactor TDD for behavior changes.
- Commit messages focus on why, not what.
- Domain language lives in `CONTEXT.md` — use Factory, Workflow Engine, Host, Adapter, Knowledge Base, etc. consistently.
